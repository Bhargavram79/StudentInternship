# Railway deployment configuration
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/*.jar"
healthcheckPath = "/api/auth/validate/1"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
